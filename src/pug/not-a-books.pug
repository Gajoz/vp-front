extends ./layout/base.pug

block meta
	- config.pageTitle = 'Не книги';
	- config.title = config.pageTitle + ' - ' + config.title;
	- config.description = 'some page description for non-books page';

block content
	include layout/catalog.pug